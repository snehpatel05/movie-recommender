services:
  - type: web
    name: reelwise
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --timeout 120 --bind 0.0.0.0:$PORT app:app
    envVars:
      - key: TMDB_API_KEY
        sync: false
